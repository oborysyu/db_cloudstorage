import { DestinationBuilder } from '../features/destinationBuilder/DestinationBuilder';

export default function App() {
  return (
    <div
      style={{
        maxWidth: 900,
        margin: '0 auto',
        padding: 24
      }}
    >
      <DestinationBuilder />
    </div>
  );
}
